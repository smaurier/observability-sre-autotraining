global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - 'rules/*.yml'

scrape_configs:
  - job_name: 'demo-app'
    static_configs:
      - targets: ['demo-app:3000']
    metrics_path: '/metrics'
